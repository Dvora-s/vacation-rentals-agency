{
  "name": "vacation-rentals-agency",
  "private": true,
  "scripts": {
    "build:client": "npm ci --prefix client && npm run build --prefix client",
    "build:server": "npm ci --prefix server",
    "start:server": "npm start --prefix server"
  },
  "engines": {
    "node": ">=20"
  }
}
