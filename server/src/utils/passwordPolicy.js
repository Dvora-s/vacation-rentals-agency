/** לפחות 8 תווים, אות אחת (גדולה או קטנה), ספרה ותו מיוחד */
export const STRONG_PASSWORD = /^(?=.*[a-zA-Z])(?=.*\d)(?=.*[^A-Za-z0-9]).{8,}$/;

export const STRONG_PASSWORD_MESSAGE =
  'הסיסמה חלשה מדי. חייבת להכיל לפחות 8 תווים, אות (גדולה או קטנה), ספרה ותו מיוחד.';
