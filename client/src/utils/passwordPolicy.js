/** לפחות 8 תווים, אות אחת (גדולה או קטנה), ספרה ותו מיוחד */
export const PASSWORD_RULE = /^(?=.*[a-zA-Z])(?=.*\d)(?=.*[^A-Za-z0-9]).{8,}$/;

export const PASSWORD_HINT =
  'לפחות 8 תווים, אות (גדולה או קטנה), ספרה ותו מיוחד';
